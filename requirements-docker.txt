# ============================================================
# requirements-docker.txt — F5-TTS-Vietnamese, CUDA 12.1
# Build environment: Linux, nvidia/cuda:12.1.1-cudnn8-runtime
# ============================================================

# ── PyTorch cu121 (CUDA 12.1 compatible) ────────────────────
# NOTE: torch+cu128 (dev machine) does NOT run on CUDA 12.1 driver.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121

# ── F5-TTS core ─────────────────────────────────────────────
vocos==0.1.0
ema-pytorch==0.7.9
x-transformers==2.19.12
torchdiffeq==0.2.5
einops==0.8.2
einx==0.4.3

# ── Config / model loading ───────────────────────────────────
hydra-core==1.3.2
omegaconf==2.3.0
cached_path==1.8.10
safetensors==0.7.0
huggingface_hub==0.36.2
transformers==4.27.4
tokenizers==0.13.3
accelerate==1.13.0

# ── Audio processing ─────────────────────────────────────────
soundfile==0.14.0
librosa==0.9.1
pydub==0.25.1
encodec==0.1.1
scipy==1.15.3
resampy==0.4.3

# ── Text processing (Vietnamese + multilingual) ───────────────
vietnormalizer==0.2.3
diskcache==5.6.3
fasttext-wheel==0.9.2
jieba==0.42.1
pypinyin==0.50.0
inflect==7.0.0
anyascii==0.3.2
Unidecode==1.3.7
num2words==0.5.12
regex==2026.5.9
cn2an==0.5.22

# ── Gradio UI ─────────────────────────────────────────────────
gradio==6.10.0
gradio_client==2.4.0

# ── Web server ───────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# ── Utilities ────────────────────────────────────────────────
numpy==1.26.4
tqdm==4.68.1
click==8.4.1
tomli==2.4.1
packaging==26.2
filelock==3.29.0
sympy==1.14.0
matplotlib==3.10.9
Pillow==12.2.0
PyYAML==6.0.3
requests==2.34.2
fsspec==2026.4.0
pandas==2.3.3
pyarrow==24.0.0
datasets==5.0.0
wandb==0.27.2
